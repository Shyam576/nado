# Jarvis AI Assistant — Python dependencies (100 % free, no API keys)
# Install with: python3 -m pip install -r requirements.txt
#
# Before installing, handle the OS-specific PyAudio requirement:
#   macOS   → brew install portaudio
#   Ubuntu  → sudo apt install portaudio19-dev python3-pyaudio -y
#   Windows → pip install pipwin && pipwin install pyaudio

# Local LLM client (talks to Ollama server)
ollama>=0.3.0

# Terminal TUI (voice mode full-screen display)
rich>=13.0.0

# Speech recognition — uses Google Web Speech API (free, no key needed)
SpeechRecognition>=3.10.4

# Offline Speech-to-Text — Whisper (local, no API key, much more accurate)
openai-whisper>=20231117
scipy>=1.11.0          # audio resampling to 16 kHz for Whisper

# Text-to-Speech
# Primary: edge-tts — Microsoft neural voices (free, no API key, very human-sounding)
#   Install: pip install edge-tts
#   List voices: edge-tts --list-voices
edge-tts>=7.0.0

# Fallback TTS (offline, used if edge-tts fails / no internet)
pyttsx3>=2.90

# Computer control
pyautogui>=0.9.54
Pillow>=10.0.0         # required by pyautogui for screenshots on non-macOS
pyscreeze>=0.1.28      # required by pyautogui for screenshots

# Audio I/O (required by SpeechRecognition for microphone access)
pyaudio>=0.2.14

# numpy — audio processing (required by Whisper and the listen() level bar)
numpy>=1.24.0

# Telegram bot transport (bot mode — python main.py bot)
# [job-queue] extra pulls in APScheduler, needed for reminder delivery polling
python-telegram-bot[job-queue]>=21.0
discord.py>=2.3.0      # Discord bot transport (runs alongside Telegram in bot mode)
pytesseract>=0.3.10     # OCR for payment-screenshot expense tracking (requires `brew install tesseract`)
python-dotenv>=1.0.0   # loads TELEGRAM_BOT_TOKEN / JARVIS_ALLOWED_CHAT_IDS from .env
